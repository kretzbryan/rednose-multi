

  <nav class="navbar navbar-expand-sm fixed-top navbar-light  nav">
    <a class="navbar-brand" href="#">Rednose</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <% if (currentUser) {%> 
          <li class="nav-item">
          <a class="nav-link nav-link-mobile" href="/home">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link nav-link-mobile" href="/browse-profiles">People</a>
        </li>
        <li class="nav-item">
          <a class='nav-link nav-link-mobile' href="#" data-toggle='modal' data-target="#addGigModal">Add Gig</a>
        </li> 
        <li class="nav-item">
          <a href="#" class="nav-link nav-link-mobile" data-toggle="modal" data-target="#logoutModal">Log Out</a>
        </li>
        <% } else { %>
          <li class="nav-item">
              <a class="nav-link nav-link-mobile" href='#' data-toggle="modal" data-target="#loginModal">Login</a>
          </li>
          <li class="nav-item">
              <a class="nav-link nav-link-mobile" href='#' data-toggle="modal" data-target="#registerModal">Sign Up</a>
          </li>
        <% } %>
      </ul>
    </div>
  </nav>

 

  <!-- Logout Modal -->
  <div class="modal fade" id="logoutModal">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="logoutModalLabel">Logout</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
            <!-- Log out form -->
            <form action="/logout?_method=DELETE" method='POST'>
            <h3 style='margin: 1rem auto 2rem auto;'>Are you sure you want to log out?</h3>
              <div class="modal-footer">
                <button type="submit" class="btn btn-primary">Log Out</button>
              </div>
          </form>
        </div>
      </div>
    </div>
  </div>
  <!-- End Logout Modal -->


 

  <% if(currentUser) { %>
  <!-- Delete Profile Modal -->
  <div class="modal fade" id="deleteProfileModal">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="deleteProfileModalLabel">Login</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
            <!-- Delete Profile Form -->
            <form action="/profile/<%= currentUser._id %>?_method=DELETE" method='POST'>
              <h5>Are you sure you want to delete your profile?</h5>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="submit" class="btn btn-primary">Delete</button>
              </div>
          </form>
        </div>
      </div>
    </div>
  </div>
  <% } %>